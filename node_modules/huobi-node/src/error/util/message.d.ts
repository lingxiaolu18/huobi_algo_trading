import { T_object } from '../../type';
/**
 * Serialize invalid map
 *
 * From:
 * { name: 'wrong_name', age: 'wrong_age' }
 * to:
 * "name='wrong_name', age='wrong_age'"
 *
 * @param map
 */
export declare function invalid_map(map: T_object): string;
